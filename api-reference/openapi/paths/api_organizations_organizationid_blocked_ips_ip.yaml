delete:
  operationId: "delete_api_organizations_organizationId_blocked_ips_ip"
  summary: "DELETE /api/organizations/{organizationId}/blocked-ips/{ip}"
  tags:
    - "Organizations"
  responses:
    "200":
      description: "Successful response"
      content:
        "application/json":
          schema:
            "$ref": "#/components/schemas/ApiResponse"
    "400":
      description: "Bad request"
      content:
        "application/json":
          schema:
            "$ref": "#/components/schemas/ApiError"
    "401":
      description: "Unauthorized"
      content:
        "application/json":
          schema:
            "$ref": "#/components/schemas/ApiError"
    "500":
      description: "Internal server error"
      content:
        "application/json":
          schema:
            "$ref": "#/components/schemas/ApiError"
  parameters:
    -
      in: "path"
      name: "organizationId"
      required: true
      schema:
        type: "string"
    -
      in: "path"
      name: "ip"
      required: true
      schema:
        type: "string"
  security:
    -
      bearerAuth:
        []
